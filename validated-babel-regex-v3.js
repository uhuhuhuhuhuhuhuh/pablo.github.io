import { formatBytes } from './codec.js';
const $=s=>document.querySelector(s),MAX=1024**4,BUD=1e7,RES=500,MAT=1024*1024;
let results=[],searchWorker=null,pathWorker=null,pathId=0,stats={attempts:0,accepted:0,rejected:0};const jobs=new Map();
function esc(v){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function vb(v){if(v.strict)return'STRICT PASS';if(v.valid)return'READABLE';if(v.recognizable)return'SIGNATURE ONLY';return'GARBLED / UNKNOWN'}
function cb(v){if(v.contentRich)return'RICH CONTENT';if(v.contentNontrivial)return'NON-TRIVIAL CONTENT';if(v.contentScore>0)return'LOW CONTENT';return'EMPTY / UNKNOWN CONTENT'}
function summary(extra=''){const e=$('#regex-search-summary');if(e)e.innerHTML=`<strong>${stats.accepted.toLocaleString()} discovered hits</strong><br><span class="muted">${stats.attempts.toLocaleString()} Babel traversal attempts · ${stats.rejected.toLocaleString()} rejected${extra?` · ${esc(extra)}`:''}.</span>`}
function row(r,i){const f=r.validation.format==='binary'?'unknown format':r.validation.format.toUpperCase(),a=r.kind==='virtual'?r.recipe:'Babel address not calculated yet',shown=a.length>520?`${a.slice(0,250)}…${a.slice(-250)}`:a;return`<article class="regex-result" data-babel-result="${i}"><div><span class="eyebrow">${r.kind==='materialized'?'Discovered materialized object':'Raw virtual traversal object'}</span><strong>${esc(r.label)}</strong><div class="regex-meta"><span>${formatBytes(r.size)}</span><span class="babel-depth">${r.depth.toLocaleString()} estimated levels</span><span>${esc(f)}</span><span>${esc(vb(r.validation))} · validation ${r.validation.score}</span><span>${esc(cb(r.validation))} · content ${r.validation.contentScore}</span><span>nonce ${Number(r.nonce).toLocaleString()}</span></div><div class="muted" style="margin-top:6px">${esc(r.validation.details)}</div><div class="muted" style="margin-top:4px"><strong>Content inspection:</strong> ${esc(r.validation.contentDetails)}</div><div class="muted" style="margin-top:4px"><strong>Filename note:</strong> this regex-matching name is a search label/expected extension, not metadata recovered from Babel.</div></div><code title="${esc(r.kind==='virtual'?r.recipe:'')}">${esc(shown)}</code><div class="button-row" style="margin-top:0">${r.kind==='materialized'?`<button class="secondary babel-calc" data-i="${i}">Calculate Babel address</button><button class="secondary babel-download" data-i="${i}">Download discovered bytes</button>`:`<button class="secondary babel-copy" data-i="${i}">Copy raw XL recipe</button>`}</div><div class="babel-address-output" style="grid-column:1/-1"></div></article>`}
function wire(){document.querySelectorAll('.babel-calc:not([data-wired])').forEach(b=>{b.dataset.wired='1';b.onclick=()=>calc(+b.dataset.i,b)});document.querySelectorAll('.babel-download:not([data-wired])').forEach(b=>{b.dataset.wired='1';b.onclick=()=>download(results[+b.dataset.i])});document.querySelectorAll('.babel-copy:not([data-wired])').forEach(b=>{b.dataset.wired='1';b.onclick=async()=>{const r=results[+b.dataset.i];await navigator.clipboard.writeText(r.recipe);const o=b.textContent;b.textContent='Copied';setTimeout(()=>b.textContent=o,900)}})}
function append(items){const list=$('#regex-result-list');if(!list)return;let h='';for(const x of items){if(x.buffer){x.bytes=new Uint8Array(x.buffer);delete x.buffer}const i=results.length;results.push(x);h+=row(x,i)}list.insertAdjacentHTML('beforeend',h);wire()}
function pworker(){if(pathWorker)return pathWorker;pathWorker=new Worker(new URL('./babel-path-worker.js?v=20260812o',import.meta.url),{type:'module'});pathWorker.onmessage=e=>{const m=e.data||{},j=jobs.get(m.id);if(!j)return;jobs.delete(m.id);const r=results[j.i],a=document.querySelector(`[data-babel-result="${j.i}"]`),b=a?.querySelector('.babel-calc'),o=a?.querySelector('.babel-address-output');if(!r||!a)return;if(!m.ok){if(b){b.disabled=false;b.textContent='Retry Babel address'}if(o)o.innerHTML=`<div class="notice error">${esc(m.error||'Babel path failed')}</div>`;return}r.path=m.path;r.depth=m.depth;const c=a.querySelector('code');if(c){c.title=m.path;c.textContent=m.path.length>520?`${m.path.slice(0,250)}…${m.path.slice(-250)}`:m.path}const d=a.querySelector('.babel-depth');if(d)d.textContent=`${m.depth.toLocaleString()} exact levels`;if(b){b.disabled=false;b.textContent='Copy Babel address'}if(o)o.innerHTML=`<div class="muted">Exact Babel address calculated from discovered bytes in ${Number(m.ms||0).toFixed(0)} ms.</div>`};return pathWorker}
function calc(i,b){const r=results[i];if(!r?.bytes)return;if(r.path){navigator.clipboard.writeText(r.path);const o=b.textContent;b.textContent='Copied';setTimeout(()=>b.textContent=o,900);return}b.disabled=true;b.textContent='Calculating in worker…';const id=++pathId,copy=r.bytes.slice();jobs.set(id,{i});pworker().postMessage({id,buffer:copy.buffer},[copy.buffer])}
function download(r){if(!r?.bytes)return;const blob=new Blob([r.bytes],{type:'application/octet-stream'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=r.label||'babel-discovered-object.bin';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function stop(msg='Search cancelled.'){if(searchWorker){searchWorker.terminate();searchWorker=null}const b=$('#regex-generate'),c=$('#regex-cancel'),s=$('#babel-regex-status');if(b)b.disabled=false;if(c)c.disabled=true;if(s)s.textContent=msg}
function start(){stop('Preparing new traversal…');results=[];stats={attempts:0,accepted:0,rejected:0};$('#regex-results').innerHTML='<div id="regex-search-summary" class="notice"><strong>0 discovered hits</strong><br><span class="muted">Starting discovery worker…</span></div><div id="regex-result-list"></div>';const b=$('#regex-generate'),c=$('#regex-cancel'),s=$('#babel-regex-status');b.disabled=true;c.disabled=false;s.textContent='Starting deterministic Babel traversal…';const settings={raw:$('#regex-input').value,count:+$('#regex-count').value,budget:+$('#regex-budget').value,minBytes:+$('#regex-min-bytes').value,maxBytes:+$('#regex-max-bytes').value,mode:$('#regex-depth-mode').value,numericStart:+$('#regex-numeric-start').value,traversalStart:+$('#regex-traversal-start').value,validationLevel:$('#regex-validation-level').value,minValidationScore:+$('#regex-min-score').value,contentRequirement:$('#regex-content-level').value,minContentScore:+$('#regex-content-score').value};const w=new Worker(new URL('./babel-regex-search-worker-v3.js?v=20260812o',import.meta.url),{type:'module'});searchWorker=w;w.onmessage=e=>{if(w!==searchWorker)return;const m=e.data||{};if(m.type==='meta'){s.textContent=m.note||'Discovery-only traversal active.';summary(m.note||'discovery-only mode')}else if(m.type==='batch'){stats={attempts:m.attempts||stats.attempts,accepted:m.accepted||stats.accepted,rejected:m.rejected||stats.rejected};append(m.items||[]);summary()}else if(m.type==='progress'){stats={attempts:m.attempts||0,accepted:m.accepted||0,rejected:m.rejected||0};s.textContent=`${stats.attempts.toLocaleString()} attempts · ${stats.accepted.toLocaleString()} hits · ${stats.rejected.toLocaleString()} rejected`;summary(`current nonce ${Number(m.currentNonce||0).toLocaleString()}`)}else if(m.type==='done'){stats={attempts:m.attempts||0,accepted:m.accepted||0,rejected:m.rejected||0};const n=Number(m.nextTraversalStart||0);$('#regex-traversal-start').value=String(n);summary(`next traversal start ${n.toLocaleString()}`);s.textContent=`Finished: ${stats.accepted.toLocaleString()} discovered hit(s) from ${stats.attempts.toLocaleString()} attempts. Next search continues at nonce ${n.toLocaleString()}.`;searchWorker=null;w.terminate();b.disabled=false;c.disabled=true;if(!stats.accepted)$('#regex-result-list').innerHTML='<div class="notice warn"><strong>No discovered files passed these constraints.</strong><br><span class="muted">That is normal for discovery-only search. Valid structured files occupy an extraordinarily tiny fraction of raw Babel byte space. No file structure or content was manufactured to force a hit.</span></div>'}else if(m.type==='error'){$('#regex-result-list').innerHTML=`<div class="notice error">${esc(m.message||'Discovery worker failed')}</div>`;stop('Discovery stopped because the constraints could not be satisfied.')}};w.onerror=e=>{$('#regex-result-list').innerHTML=`<div class="notice error">${esc(e.message||'Discovery worker crashed')}</div>`;stop('Discovery worker stopped.')};w.postMessage({type:'start',settings})}
function install(){const p=$('#panel-regex'),t=document.querySelector('[data-tab="regex"]');if(!p||!t)return;t.textContent='Babel Discovery';p.innerHTML=`<div class="card"><span class="eyebrow">Regex label → deterministic Babel traversal → decode bytes → validate → inspect content</span><h3>Babel discovery search</h3><p class="help">This mode does not construct PDFs, images, archives, model files, or text. It traverses deterministic raw byte objects in Babel space and only returns an object when those bytes naturally pass your file-format and content filters.</p><div class="notice"><strong>Filename semantics:</strong> Babel's core mapping is bytes ↔ path and does not contain arbitrary filenames. The regex enumerates search labels and supplies an expected extension for validation. A hit's bytes and Babel address are the discovered object; the label itself is not recovered metadata.</div><div class="grid-2" style="margin-top:16px"><div><label for="regex-input">Filename/search-label regex</label><input id="regex-input" class="mono" type="text" value="EFTA[0-9]{8}\\.pdf"></div><div><label for="regex-count">Maximum hits to return</label><input id="regex-count" type="number" min="1" max="${RES}" value="100"></div></div><div class="grid-3" style="margin-top:16px"><div><label for="regex-min-bytes">Minimum bytes</label><input id="regex-min-bytes" type="number" min="0" max="${MAX}" value="10000"></div><div><label for="regex-max-bytes">Maximum bytes (up to 1 TiB)</label><input id="regex-max-bytes" type="number" min="1" max="${MAX}" value="80000"></div><div><label for="regex-depth-mode">Size/depth bias</label><select id="regex-depth-mode" class="input"><option value="deep" selected>Bias larger/deeper</option><option value="max">Always maximum bytes</option><option value="balanced">Balanced</option></select></div><div><label for="regex-budget">Traversal attempts (up to 10,000,000)</label><input id="regex-budget" type="number" min="1" max="${BUD}" value="5000"></div><div><label for="regex-numeric-start">Numeric label start / offset</label><input id="regex-numeric-start" type="number" min="0" value="0"></div><div><label for="regex-traversal-start">Babel traversal start / nonce</label><input id="regex-traversal-start" type="number" min="0" value="0"></div></div><div class="grid-3" style="margin-top:16px"><div><label for="regex-validation-level">Validation requirement</label><select id="regex-validation-level" class="input"><option value="any">Any raw bytes</option><option value="signature">Recognizable signature</option><option value="readable">Structurally valid / readable</option><option value="strict" selected>Strict materialized validation</option></select></div><div><label for="regex-min-score">Minimum validation score (0–100)</label><input id="regex-min-score" type="number" min="0" max="100" value="80"></div><div><label for="regex-content-level">Content requirement</label><select id="regex-content-level" class="input"><option value="any">Any content</option><option value="nontrivial" selected>Non-trivial / visibly populated</option><option value="rich">Rich content only</option></select></div><div><label for="regex-content-score">Minimum content score (0–100)</label><input id="regex-content-score" type="number" min="0" max="100" value="60"></div><div><label>Full inspection ceiling</label><div class="notice" style="margin:0;padding:11px 13px">${formatBytes(MAT)} per candidate</div></div></div><div class="notice" style="margin-top:16px"><strong>Discovery reality:</strong> a raw candidate matching <code>%PDF-</code> by chance is about 1 in 2<sup>40</sup> before checking objects, xref, pages, or useful content. PNG's 8-byte signature alone is 1 in 2<sup>64</sup>. A strict/content-rich search can legitimately return zero results even after millions of attempts.</div><div class="notice" style="margin-top:12px"><strong>No injected content:</strong> the discovery worker never inserts headers, page text, pixels, archive entries, tensor data, or the regex label into candidate bytes. Signature checks only reject impossible candidates faster; they never modify bytes.</div><div class="button-row"><button id="regex-generate">Search Babel byte space</button><button id="regex-cancel" class="secondary" disabled>Cancel search</button></div><div id="babel-regex-status" class="muted" style="margin-top:12px">Ready.</div></div><div id="regex-results"><div class="notice">Choose a label pattern, size range, validation, and content conditions, then traverse Babel byte space.</div></div>`;$('#regex-generate').onclick=start;$('#regex-cancel').onclick=()=>stop();$('#regex-input').addEventListener('keydown',e=>{if(e.key==='Enter')start()})}
install();